options(repos = structure(c(CRAN = "https://cran.rstudio.com")),
        browser = NULL,
        viewer = NULL)

source(file.path(if (.Platform$OS.type == "windows") file.path(Sys.getenv("HOMEDRIVE"), Sys.getenv("HOMEPATH")) else Sys.getenv("HOME"), ".vscode-R", "init.R"))

options(vsc.plot = "Two",
        vsc.dev.args = list(width = 2560, height = 1440),
        vsc.browser = FALSE,
        vsc.viewer = FALSE,
        vsc.page_viewer = FALSE)
